const path = require("path");
const PATH_SRC = path.resolve(__dirname, "../../src");
const PATH_OUT = path.resolve(__dirname, "../../out");
const PATH_ROOT = path.resolve(__dirname, "../../../");

module.exports = { PATH_ROOT, PATH_SRC, PATH_OUT };
